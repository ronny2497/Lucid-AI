{
  "name": "@lucid/adapter-hermes",
  "version": "0.0.0",
  "description": "@lucid/adapter-hermes — the hermes reference adapter (TypeScript, reference/test-only). Exercises the FULL 10-event HSC taxonomy through @lucid/sdk, including the tool.call{mutated_state:true} -> verify.result pairing AND a mutating tool.call with no verify.result (the honest empty-feedback case). Nothing hermes-specific leaks into Lucid core (D-07).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lucid/sdk": "workspace:*",
    "@lucid/hsc-schema": "workspace:*"
  },
  "devDependencies": {
    "@lucid/conformance": "workspace:*",
    "@opentelemetry/api": "~1.9.0",
    "@opentelemetry/sdk-trace-node": "^2.8.0",
    "typescript": "5.3.3",
    "vitest": "^2.1.9"
  }
}
