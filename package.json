{
  "name": "lucid-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Lucid monorepo root — pnpm workspace for the HSC standard, SDK, collector, store, and diagnostics packages.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  }
}
